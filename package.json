{
  "name": "my-help-docs",
  "version": "1.0.0",
  "description": "绝区零 Mod 帮助文档",
  "scripts": {
    "docs:dev": "vitepress dev",
    "docs:build": "vitepress build",
    "docs:preview": "vitepress preview",
    "sync": "npx feishu-pages && node scripts/post-sync.mjs",
    "deploy": "node scripts/deploy.mjs"
  },
  "devDependencies": {
    "vitepress": "^1.6.4"
  },
  "dependencies": {
    "feishu-pages": "^0.7.6"
  }
}
